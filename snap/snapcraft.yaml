name: number-guess
version: git
summary: number-guess
description: |
  python3 number guessing game

grade: devel
confinement: devmode

apps:
  number-guess:
    command: python3 $SNAP/bin/number-guess.py

parts:
  number-guess:
    source: https://github.com/ul7r41337h4x0r/guessprogram
    source-type: git
    plugin: python
    python-version: python3

